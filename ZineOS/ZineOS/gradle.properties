# Increase memory to avoid OutOfMemoryError during dexing
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8 -XX:+UseParallelGC

# Enable AndroidX support (Fixes your new red error)
android.useAndroidX=true